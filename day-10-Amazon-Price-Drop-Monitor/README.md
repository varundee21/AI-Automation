Built: A personal n8n automation that checks an Amazon product price against a target price and sends an email alert when the price drops to or below the target.

Workflow:
Manual Trigger/Scheduled Trigger → Edit Fields → HTTP Request (Amazon) → Code (Extract Price) → IF → Gmail
