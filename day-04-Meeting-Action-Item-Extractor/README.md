Built an n8n workflow that uses Google Gemini to extract action items from meeting notes and converts the AI-generated JSON into a clean, readable email.

Workflow:
Gmail Trigger → Gemini → Code node → Gmail
